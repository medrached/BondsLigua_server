package tn.esprit.bondsLiga.bondsLigua_server.services;

import java.io.IOException;
import java.math.BigDecimal;
import java.util.ArrayList;
import java.util.List;



public class YahooFinanceRemote implements IYahooFinanceRemote {
/*
	@Override
	public List<BigDecimal> listOfCompanies() throws IOException  {
		Stock stock = YahooFinance.get("INTC");

		BigDecimal price = stock.getQuote().getPrice();
		BigDecimal change = stock.getQuote().getChangeInPercent();
		BigDecimal peg = stock.getStats().getPeg();
		BigDecimal dividend = stock.getDividend().getAnnualYieldPercent();
		List<BigDecimal> bigDecimals=new ArrayList<>();
		bigDecimals.add(price);
		bigDecimals.add(change);
		bigDecimals.add(peg);
		bigDecimals.add(dividend);
		
		return bigDecimals;
	
		
	}*/

	
	
}
